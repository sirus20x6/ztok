# Pinned to versions that exist in the current run-results table in
# bench/RESULTS.md. Bump deliberately when re-running the cross-bench.
tiktoken==0.12.0
tokenizers==0.21.4
tokenmonster==1.1.12
sentencepiece==0.2.1
